# Dependencies for the COMPS workflow — installed both locally and inside the container.
# Install with: pip install --pre -r requirements.txt  (--pre needed for laser_measles)

# Model 
laser_measles

# COMPS orchestration (version-sensitive — pin tightly)
idmtools~=3.0
idmtools-platform-comps~=3.0
idmtools-models~=3.0
idm_pycomps~=2.11
