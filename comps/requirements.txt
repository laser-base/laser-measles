# Local orchestration dependencies for COMPS submission workflow.
# These are NOT installed inside the container (see Dockerfile for that).
# Install with: pip install -r requirements.txt

idmtools==3.0.8
idmtools-platform-comps==3.0.8
idm_pycomps==2.11.0
polars>=1.30.0
matplotlib>=3.8.0
numpy>=2.0.0
